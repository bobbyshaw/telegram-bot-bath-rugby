'use strict';

const assert = require('assert');
const sinon = require('sinon');
const Intent = require('../lib/intent');

describe('The Intents component', function () {
    it ('Initialisation', function() {
        var intents = new Intent();

    });
});
